{
  "name": "poppit",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "description": "Poppit Full Stack Framework",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "./node_modules/.bin/tsc --version",
    "dev": "pnpm --filter @poppit/server dev",
    "start": "pnpm --filter @poppit/server start",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}